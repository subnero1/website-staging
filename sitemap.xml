---
layout: null
---
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <!-- Homepage -->
  <url>
    <loc>https://subnero.com/</loc>
    <lastmod>{{ site.time | date_to_xmlschema }}</lastmod>
    <changefreq>weekly</changefreq>
    <priority>1.0</priority>
  </url>
  
  <!-- Gen4x Product Series (High Priority - New Product Line) -->
  {% for page in site.pages %}
    {% if page.url contains '/gen4x/' and page.sitemap != false %}
      {% if page.url == '/products/gen4x/' %}
        <url>
          <loc>{{ "https://subnero.com" | append: page.url | replace: '//', '/' | replace: ':/', '://' }}</loc>
          <lastmod>{{ page.last_modified_at | default: site.time | date_to_xmlschema }}</lastmod>
          <changefreq>monthly</changefreq>
          <priority>0.9</priority>
        </url>
      {% elsif page.url contains '/gen4x/wnc-' or page.url contains '/gen4x/wnc-s1000-n' %}
        <url>
          <loc>{{ "https://subnero.com" | append: page.url | replace: '//', '/' | replace: ':/', '://' }}</loc>
          <lastmod>{{ page.last_modified_at | default: site.time | date_to_xmlschema }}</lastmod>
          <changefreq>monthly</changefreq>
          <priority>0.8</priority>
        </url>
      {% else %}
        <url>
          <loc>{{ "https://subnero.com" | append: page.url | replace: '//', '/' | replace: ':/', '://' }}</loc>
          <lastmod>{{ page.last_modified_at | default: site.time | date_to_xmlschema }}</lastmod>
          <changefreq>monthly</changefreq>
          <priority>0.7</priority>
        </url>
      {% endif %}
    {% endif %}
  {% endfor %}
  
  <!-- All Other Pages -->
  {% for page in site.pages %}
    {% if page.sitemap != false and page.url != "/404.html" %}
      {% unless page.url contains '/gen4x/' %}
        <url>
          <loc>{{ "https://subnero.com" | append: page.url | replace: '//', '/' | replace: ':/', '://' }}</loc>
          <lastmod>{{ page.last_modified_at | default: site.time | date_to_xmlschema }}</lastmod>
          <changefreq>monthly</changefreq>
          <priority>0.7</priority>
        </url>
      {% endunless %}
    {% endif %}
  {% endfor %}
  
  <!-- Blog Posts -->
  {% for post in site.posts %}
    <url>
      <loc>{{ "https://subnero.com" | append: post.url | replace: '//', '/' | replace: ':/', '://' }}</loc>
      <lastmod>{{ post.date | date_to_xmlschema }}</lastmod>
      <changefreq>monthly</changefreq>
      <priority>0.5</priority>
    </url>
  {% endfor %}
</urlset>
